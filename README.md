# LaunchBox Global - MERN Frontend

A clean **MERN stack frontend** built with React, Vite, Tailwind CSS, and React Router DOM.

## 🚀 Tech Stack

- **React 19** - UI Library
- **Vite** - Build Tool & Dev Server
- **React Router DOM** - Client-side Routing
- **Tailwind CSS** - Styling
- **GSAP** - Animations
- **Lucide React** - Icons

## 📁 Project Structure

```
/src
  /components      # Reusable UI components
  /pages          # Page components
  /layout         # Layout components
  /assets         # Images, videos, logos
  /hooks          # Custom React hooks
  App.jsx         # Main app with routing
  main.jsx        # Entry point
```

## ✅ No Next.js

This is a **pure React frontend** using:
- ✅ Vite (not Next.js)
- ✅ React Router DOM (not Next.js routing)
- ✅ Client-side rendering only
- ✅ No server components
- ✅ No `app/` directory
- ✅ No `pages/` directory (Next.js style)

## 🛠️ Commands

```bash
npm run dev      # Start development server
npm run build    # Build for production
npm run preview  # Preview production build
```

## 📦 Dependencies

All frontend-only dependencies:
- `react` & `react-dom`
- `react-router-dom`
- `vite` & `@vitejs/plugin-react`
- `tailwindcss`
- `gsap` (animations)
- `lucide-react` (icons)

## 🌐 Pages

- `/` - Home
- `/services` - Services listing
- `/services/:slug` - Service details
- `/case-studies` - Case studies
- `/industries` - Industries
- `/blog` - Blog listing
- `/blog/:slug` - Blog post
- `/contact` - Contact page

## 🎨 Features

- Responsive design
- Smooth animations (GSAP)
- Dropdown menus
- Mobile navigation
- Scroll animations
- Interactive components

---

**Built with Vite + React** - No Next.js, no server-side rendering, pure client-side React app.
